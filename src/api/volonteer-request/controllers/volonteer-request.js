'use strict';

/**
 * volonteer-request controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::volonteer-request.volonteer-request');
